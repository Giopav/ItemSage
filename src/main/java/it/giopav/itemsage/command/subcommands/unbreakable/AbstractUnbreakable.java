package it.giopav.itemsage.command.subcommands.unbreakable;

import it.giopav.itemsage.command.superclasses.AbstractCommand;

public class AbstractUnbreakable extends AbstractCommand {
    protected AbstractUnbreakable() {
        super(argsArray[9]);
    }
}
